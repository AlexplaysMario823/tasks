let list = {
     data: ['Brian', 'John', 'Dave', 'Phil', null, null], // using an array
     insert (pos, item) {
        // your code here
        // refer to data as 'this.data' eg this.data[0] for 'Brian'
        if (not (data[5] === null)) {
            //it's full
        }
        else {
            for (let x=0;x<this.data.length;i--) {

            }
        }
     }
    }
    list.insert(2, 'Eric')
    console.log(list.data) // Brian, John, Eric, Dave, Phil, null
    